
class ClassRegulator:

    def __init__(self, CLASS_NAME, METHOD):
        self.CLASS_NAME
        self.METHOD

    def class_name_reg():
        #regulate the class name with regular expressions

    def method_name_reg():
        #relulate the method name with regular expressions

    def viewmodel_flag():
        #define the class viewmodel flag convention

    def model_flag():
        #define the model flag regular expression
