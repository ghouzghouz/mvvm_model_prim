class Router:

    def __init__(self, ROUTE, APP_USER):
        self.ROUTE
        self.APP_USER

    def get():
        #Push the router to know the right path

    def call(self):
        #Invoque The right functionality from the core moter

    def read(self, VIEW_MODEL, VIEW):
        #Know the viewmodel name and the view name

    def connect(self, VIEW_MODEL, VIEW_NAME):
        #Connect the viewmodel and the view

    def dispatch(self):
        #Display resources into the user screen
